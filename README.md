# Debian router - repository
Packed and preconfigured versions of `debian-router` packages.  
For more info, see `debian-router` repo.

# Under construction
This repo will be removed (see `debian-router-test` repo)
